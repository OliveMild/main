# main

A simple Python greeting script.

## Usage

Run the script directly:

```bash
python hello.py
# Output: Hello, World!

python hello.py Alice
# Output: Hello, Alice!
```

## Features

- `greet(name)` — Returns a greeting string for the given name.
- CLI argument support — pass a name as an argument, or omit it to greet the world.