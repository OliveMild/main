# main

A simple Python greeting application.

## Usage

Run with no arguments to greet the world:

```bash
python hello.py
```

Run with a name argument to greet a specific person:

```bash
python hello.py Alice
```