# main

A simple Python project that prints "Hello World".

## Usage

Run the script with:

```bash
python3 hello.py
```

## Files

- **`hello.py`**: Entry point that prints `Hello World` when executed directly.
