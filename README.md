# main

A simple Python greeting application.

## Usage

```bash
# Print a default greeting
python hello.py

# Greet a specific person
python hello.py Alice

# Use a time-based greeting (Good morning/afternoon/evening/night)
python hello.py --time
python hello.py Alice --time
```