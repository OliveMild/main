# main

A simple Python project that prints a greeting message.

## Usage

Run the script directly:

```bash
python hello.py
```

To greet a specific name, import and call the `greet` function:

```python
from hello import greet

print(greet("Alice"))  # Hello Alice
print(greet())         # Hello World
```
