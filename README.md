# main

A simple Python "Hello World" project.

## Usage

```bash
python3 hello.py
```

This prints `Hello World` to the terminal.

## Files

- `hello.py` — Entry point that prints "Hello World" when run directly.
