# main

A simple Python greeting application.

## Usage

Run without arguments to print a generic greeting:

```bash
python hello.py
```

Run with a name argument to print a personalized greeting:

```bash
python hello.py Alice
```